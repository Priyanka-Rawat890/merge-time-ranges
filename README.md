# 🧮 Merge Time Ranges Assignment

This repository contains the solution for the **"Merge Discontinuous Time Ranges"** assignment.

---

## 📂 Files Included
- **`my-module.js`** → Contains the logic to merge overlapping or continuous time ranges.  
- **`test.js`** → Test script that runs multiple sample inputs and prints the merged results.

---

## ⚙️ How to Run the Code

### 1. Install Node.js
If Node.js is not installed, download it from [https://nodejs.org/](https://nodejs.org/)  
(Choose **LTS version** and install with default options).

### 2. Open Terminal / Command Prompt
Navigate to your project folder. Example:
```bash
cd Desktop/merge-time-ranges
